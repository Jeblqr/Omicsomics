# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=omicsomics
POSTGRES_PORT=5432

# MinIO Object Storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme123
MINIO_BUCKET=omicsomics
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Redis
REDIS_PORT=6379

# Backend API
BACKEND_PORT=8001
LOG_LEVEL=INFO

# Frontend
FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8001

# Cloudflared Tunnel (Optional)
# Get your token from: https://dash.cloudflare.com/
# CLOUDFLARED_TOKEN=your_cloudflared_tunnel_token_here

# 注意：
# 1. 复制此文件为 .env 并填入真实值
# 2. docker-compose.yml 使用环境变量（安全）
# 3. docker-compose.local.yml 包含实际配置（不会提交到git）
